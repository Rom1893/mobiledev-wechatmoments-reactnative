import {ImageStyle, TextStyle, ViewStyle} from 'react-native';

export type BasicStyle = {
  container: ViewStyle;
  text: TextStyle;
  image: ImageStyle;
  avatar: ImageStyle;
  commentsWrapper: ViewStyle;
  comment: TextStyle;
  commentSender: TextStyle;
  commentIcon: ViewStyle;
};
